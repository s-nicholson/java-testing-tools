package com.example.testing.pitest;

public class Goldilocks {
    public boolean likesPorridge(int temp) {
        // I have no idea if this is actually the right temperature for porridge...
        return temp > 40 && temp <= 60;
    }
}
